image: alpine/latest
packages:
  - cmake
  - imagemagick
  - imagemagick-dev
  - ninja
sources:
  - https://github.com/KnightOS/kimg
tasks:
  - build: |
      cd kimg
      mkdir build && cd build
      cmake .. -GNinja
      ninja
