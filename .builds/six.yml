image: debian/stable
packages:
  - cmake
  - imagemagick
  - libmagickwand-dev
  - ninja-build
  - asciidoc
sources:
  - https://github.com/KnightOS/kimg
tasks:
  - build: |
      cd kimg
      mkdir build && cd build
      cmake .. -GNinja
      ninja
